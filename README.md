# Cmd_compiler
"Cmd compiler" is a Windows prompt script compiler.Have many options.
1.Create\start a script and change,can run,compiler,clear and exit;
2.have Cmd User Guide;
3.have Log function,you can view logs at any time.


size is very little,about 2MB
